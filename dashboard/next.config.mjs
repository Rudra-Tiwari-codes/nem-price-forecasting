/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // React Compiler disabled - causes hydration mismatch errors with Next.js 16.1.1
  // reactCompiler: true,
};

export default nextConfig;
